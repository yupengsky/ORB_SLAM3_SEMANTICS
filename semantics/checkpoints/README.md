# Semantic Model Checkpoints

Model weights are intentionally ignored by Git.

Place segmentation weights here, then set `yolo_model` in `local/dataset_config.json`.
The default expected path is:

```text
semantics/checkpoints/yolo26l-seg.pt
```

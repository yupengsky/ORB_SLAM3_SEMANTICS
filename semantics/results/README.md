# Semantic Results

Generated semantic maps, navigation JSON files, sketches, videos, and smoke-test outputs are intentionally ignored by Git.

Use `local/results/` or another path configured in `local/dataset_config.json` for local experiments.
